<%= simple_form_for([@choogle, @proposal]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :place_id, label: "Select a place" %>
  <%= f.input :tag_id, label: "Tag this place", collection: Tag.all %>

  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
