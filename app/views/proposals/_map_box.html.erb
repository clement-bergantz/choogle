<div class="banner" id="image-proposal-map" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.3) 0%, rgba(0,36,61,0.3) 0%), url('#');">
   <div class="container text-center">
     <h3><%= proposal.place.name %></h3>
     <p><%= "Open now" unless proposal.place.placify.opening_hours["open_now"].empty? %></p>
     <p> <%= proposal.place.placify.rating unless proposal.place.placify.rating.nil? %> </p>
   </div>
 </div>
